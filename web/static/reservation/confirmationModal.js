'use static';

/*angular.module('myApp.confirmationModal', ['ui.bootstrap.modal']).controller('ModalInstanceCtrl', 
function ($uibModalInstance, $scope) {
  var $ctrl = this;
  //$ctrl.items = items;

  $ctrl.ok = function () {
    $uibModalInstance.close($ctrl.item);
  };

  $ctrl.cancel = function () {
    $uibModalInstance.dismiss('cancel');
  };
});
*/
